# 示例：认证功能设计

## 正确示例
- 关联需求：`req-auth-001`
- 关键组件：`auth-service`、`user-service`、`rate-limit-store`
- 接口契约：定义登录接口输入输出、错误码、幂等语义。
- 数据设计：记录失败次数与锁定时间字段。
- 回滚策略：支持新旧令牌并行验证 7 天。

## 常见反例
- 反例 1：只重复需求 FR 文本，不提供接口或数据设计。
- 反例 2：新增业务语义但未回填需求或 ADR。

## 填写要点
- 设计必须逐条映射需求并解释如何实现。
- 性能、安全、运维、回滚应给出可执行方案。
